
const ChatBox = () => {
  return <h1>ChatBox</h1>;
};

// Send button: Sends a chat message
// Cancel button: Cancels a message draft
// Edit button: Allows a user to edit their previously sent message
// Delete button: Deletes the message from the user’s view (not the other user’s) and deletes a user's own messages from both users' views

export default ChatBox;